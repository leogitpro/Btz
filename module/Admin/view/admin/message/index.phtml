<?php
$this->pageTitleBar('消息内容管理');
$this->sideTreeMenu()->setActiveId($activeId);

?>


<div class="row">
    <div class="col-lg-12">

        <table class="table table-hover">
            <tr>
                <th>主题</th>
                <th>时间</th>
                <th>操作</th>
            </tr>
            <?php foreach($rows as $row) { if ($row instanceof \Admin\Entity\MessageContent) { ?>
                <tr>
                    <td><?php echo $row->getTopic(); ?></td>
                    <td><?php echo $row->getCreated()->format('Y-m-d H:i:s'); ?></td>
                    <td>
                        <a href="<?php echo $this->url('admin/message', ['action' => 'delete', 'key' => $row->getId()]); ?>">
                            <i class="fa fa-close fa-fw" aria-hidden="true"></i> 删除
                        </a>
                    </td>
                </tr>
            <?php } } ?>

        </table>

    </div>
</div>

<?php echo $this->pagination()->render(); ?>
